server {
    listen 8081;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location /docs/ {
        try_files $uri $uri/ /docs/index.html;
    }

    location / {
        return 302 /docs/;
    }
}
